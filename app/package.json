{
    "name": "app",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build && bun scripts/build-template-index.ts",
        "preview": "vite preview",
        "build:index": "bun scripts/build-template-index.ts"
    },
    "dependencies": {
        "fuse.js": "^7.3.0",
        "preact": "^10.29.1"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.10.5",
        "@types/node": "^24.12.2",
        "typescript": "~6.0.2",
        "vite": "^8.0.9"
    }
}